<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.ipd.tankking.fragment.FeedBackFragment">

    <LinearLayout
        android:id="@+id/ll_feed_back"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="@dimen/y50"
        android:background="@drawable/bg_personal"
        android:orientation="vertical"
        android:paddingBottom="@dimen/y50"
        android:paddingEnd="@dimen/x50"
        android:paddingStart="@dimen/x50"
        android:paddingTop="@dimen/y50"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHeight_percent="0.6"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintWidth_percent="0.9">

        <EditText
            android:id="@+id/et_feed_back"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="top"
            android:layout_marginBottom="@dimen/y50"
            android:background="@null"
            android:hint="请输入您反馈的详细内容（500字以内）"
            android:lineSpacingExtra="@dimen/y30"
            android:textColor="@color/white"
            android:textColorHint="@color/tx_hint"
            android:textSize="16sp"
            android:textStyle="bold" />
    </LinearLayout>

    <ImageButton
        android:id="@+id/ib_feed_back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/y100"
        android:background="@null"
        android:src="@mipmap/ic_commit"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/ll_feed_back" />
</android.support.constraint.ConstraintLayout>
